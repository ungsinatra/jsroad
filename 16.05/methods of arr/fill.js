// Fill. Напишите функцию, которая заполняет новый массив предоставленным значением.
const fill = (arraySize, value) => {
	let arr =[1];
	let newArr =arr.fill(valueToFill,0,arr.length=data)

	return newArr
};

const data = 4;
const valueToFill = 'a';
console.log(fill(data, valueToFill)); // ['a', 'a', 'a']